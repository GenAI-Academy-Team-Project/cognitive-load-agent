'use client';
import { PaginatedList } from './paginated-list';

import { useCallback, useEffect, useState } from 'react';
import { CalendarDays, CheckCircle2, ExternalLink, LoaderCircle, Pencil, Plus, RefreshCw } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { Calendar } from '@/components/ui/calendar';
import { Popover, PopoverContent, PopoverTrigger } from '@/components/ui/popover';
import { Input } from '@/components/ui/input';
import { TimePicker } from '@/components/ui/time-picker';
import { Badge } from '@/components/ui/badge';
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from '@/components/ui/dialog';
import { localInput } from '@/lib/calendar-time';
import { NotificationComposer } from './notification-composer';
import { calendarUpdateDraft } from '@/lib/calendar-update';
import type { CalendarAction, CalendarAppointment, CalendarState } from '@/lib/calendar-types';
import type { DashboardState } from '@/lib/types';

const selectClass = 'h-10 w-full rounded-lg border bg-background px-3 text-sm outline-none focus:ring-2 focus:ring-ring';
const formatWhen = (value: string, timeZone: string) => new Intl.DateTimeFormat('en-CA', { dateStyle: 'medium', timeStyle: 'short', timeZone }).format(new Date(value));
function calendarLink(value?: string | null) {
  try { const url = new URL(value || ''); return url.protocol === 'https:' && ['calendar.google.com', 'www.google.com'].includes(url.hostname) ? url.href : null; } catch { return null; }
}

export function CareCalendar({ state, onCompleted, taskId }: { state: DashboardState; onCompleted: () => void; taskId?: string }) {
  const recipientId = state.selectedRecipient.id;
  const canWrite = state.currentUser.role !== 'viewer' && state.consent.status === 'active';
  const [data, setData] = useState<CalendarState | null>(null);
  const [error, setError] = useState('');
  const [notice, setNotice] = useState('');
  const [busy, setBusy] = useState(false);
  const [editor, setEditor] = useState<CalendarAppointment | 'new' | null>(null);
  const [proposal, setProposal] = useState<CalendarAction | null>(null);
  const [disconnectOpen, setDisconnectOpen] = useState(false);
  const [updateAction, setUpdateAction] = useState<CalendarAction | null>(null);
  const refresh = useCallback(async () => {
    const response = await fetch(`/api/calendar?recipientId=${encodeURIComponent(recipientId)}`);
    const result = await response.json() as CalendarState;
    if (!response.ok) throw new Error(result.error || 'Calendar could not be loaded.');
    setData(result);
  }, [recipientId]);
  useEffect(() => {
    let active = true;
    const url = new URL(window.location.href);
    const callbackNotice = url.searchParams.get('calendarNotice') || '';
    if (callbackNotice) { url.searchParams.delete('calendarNotice'); window.history.replaceState(null, '', url); }
    fetch(`/api/calendar?recipientId=${encodeURIComponent(recipientId)}`).then(async (response) => {
      const result = await response.json() as CalendarState;
      if (!response.ok) throw new Error(result.error || 'Calendar could not be loaded.');
      if (active) { setData(result); setNotice(callbackNotice); }
    }).catch((e) => { if (active) setError(e.message); });
    return () => { active = false; };
  }, [recipientId]);

  async function act(action: string, payload: Record<string, unknown> = {}) {
    setBusy(true); setError(''); setNotice('');
    try {
      const response = await fetch('/api/calendar', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ action, recipientId, ...payload }) });
      const result = await response.json() as { error?: string; url?: string };
      if (!response.ok) throw new Error(result.error || 'Calendar action failed.');
      if (result.url) { window.location.assign(result.url); return true; }
      await refresh();
      if (action === 'approve') { setNotice('Google Calendar and the care plan are updated. You can prepare a caregiver update below.'); onCompleted(); }
      if (action === 'review_recovery') setNotice('Review the remaining care changes, then approve to finish. Google is already updated.');
      if (action === 'select_calendar') setNotice('Calendar selected for this recipient.');
      if (action === 'disconnect') setNotice('Google account disconnected from Carestead. Existing Google events remain on their calendars.');
      return true;
    } catch (e) {
      setError(e instanceof Error ? e.message : 'Calendar action failed.');
      await refresh().catch(() => {});
      return false;
    } finally { setBusy(false); }
  }
  const connected = data?.connection?.status === 'connected' && data.configured;
  const unresolved = data?.actions.filter((action) => !['executed', 'rejected'].includes(action.status)) || [];
  const activeAppointments = data?.appointments.filter((appointment) => appointment.status === 'confirmed') || [];
  const selectedTask = state.tasks.find(task => task.id === taskId);
  const selectedAppointment = activeAppointments.find(appointment => appointment.task_id === taskId);
  const calendarReady = Boolean(connected && data?.binding);
  const setupMessage = !data ? 'Checking Google Calendar setup…' : !data.configured ? 'Google Calendar setup is required for scheduling, rescheduling, and cancellation. An owner can enable it in Account settings → Integrations.' : !connected ? 'Connect your Google Calendar account to schedule, reschedule, or cancel appointments.' : !data.binding ? 'Choose a calendar below to enable scheduling, rescheduling, and cancellation for this person.' : '';
  const actionReady = (action: CalendarAction) => calendarReady && data?.binding?.calendar_id === action.payload.calendarId;

  return <>
    <div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-start">
      <div><p className="text-xs font-semibold uppercase tracking-widest text-primary">{state.selectedRecipient.display_name}&apos;s care plan</p><h1 className="mt-2 font-heading text-3xl font-semibold tracking-tight">Calendar</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-muted-foreground">Schedule care appointments, review invitations, and keep the care plan up to date.</p></div>
      <Button disabled={busy || !canWrite || !calendarReady} title={setupMessage || undefined} onClick={() => setEditor('new')}><Plus /> Schedule appointment</Button>
    </div>
    {setupMessage && <output className="mt-5 block rounded-xl border bg-secondary p-4 text-sm">{setupMessage}</output>}
    {error && <p role="alert" className="mt-5 rounded-xl border border-destructive/30 bg-destructive/5 p-4 text-sm text-destructive">{error}</p>}
    {notice && <output className="block mt-5 rounded-xl border bg-secondary p-4 text-sm text-primary">{notice}</output>}
    {!data ? <output className="mt-8 flex items-center gap-2 text-sm text-muted-foreground"><LoaderCircle className="size-4 animate-spin" /> Loading calendar…</output> : <>
      <section aria-label="Google Calendar connection" className="mt-8 rounded-[22px] border bg-card p-5 sm:p-6">
        <div className="flex flex-wrap items-start justify-between gap-4"><div className="flex gap-3"><span className="grid size-10 shrink-0 place-items-center rounded-xl bg-secondary text-primary"><CalendarDays className="size-5" /></span><div><h2 className="font-heading text-lg font-semibold">Google Calendar</h2><p className="mt-1 text-sm text-muted-foreground">{data.connection?.email || 'Connect the Google account you use for care appointments.'}</p><p className="mt-1 text-xs text-muted-foreground">Calendar access sends invitations. Your Gmail inbox is not accessed.</p></div></div><Badge variant="outline">{connected ? 'Connected' : data.connection?.status === 'reconnect_required' ? 'Reconnect required' : 'Not connected'}</Badge></div>
        {!data.configured && <p className="mt-4 text-sm text-muted-foreground">Google Calendar is off or needs credentials. An owner can review it in Integrations. Your local tasks and timeline still work.</p>}
        {data.error && <p role="alert" className="mt-4 text-sm text-destructive">{data.error}</p>}
        {!canWrite && <p className="mt-4 text-sm text-muted-foreground">{state.consent.status !== 'active' ? 'Calendar actions are paused while recipient consent is withdrawn.' : 'Owners and caregivers can connect accounts and schedule appointments.'}</p>}
        <div className="mt-5 flex flex-wrap items-end gap-3">
          {connected && <div className="min-w-0 flex-1 sm:max-w-md"><label htmlFor="recipient-calendar" className="mb-2 block text-sm font-medium">Calendar for {state.selectedRecipient.display_name}</label><select id="recipient-calendar" className={selectClass} value={data.binding?.calendar_id || ''} disabled={busy || !canWrite || !data.calendars.length} onChange={(event) => act('select_calendar', { calendarId: event.target.value })}><option value="" disabled>Choose your calendar</option>{data.binding && !data.calendars.some((calendar) => calendar.id === data.binding!.calendar_id) && <option value={data.binding.calendar_id}>{data.binding.calendar_name}</option>}{data.calendars.map((calendar) => <option key={calendar.id} value={calendar.id}>{calendar.summary}</option>)}</select></div>}
          {!connected && <Button disabled={busy || !canWrite || !data.configured} onClick={() => act('connect')}>{data.connection?.status === 'reconnect_required' ? 'Reconnect Google Calendar' : 'Connect Google Calendar'}</Button>}
          {connected && <Button variant="outline" disabled={busy} onClick={() => setDisconnectOpen(true)}>Disconnect</Button>}
          <Button variant="ghost" disabled={busy} onClick={async () => { setBusy(true); try { await refresh(); setError(''); } catch (e) { setError(e instanceof Error ? e.message : 'Refresh failed.'); } finally { setBusy(false); } }}><RefreshCw /> Refresh</Button>
        </div>
      </section>
      {selectedTask && <section aria-label="Selected care responsibility" className="mt-6 rounded-2xl border bg-secondary p-5"><h2 className="font-heading text-lg font-semibold">{selectedTask.title}</h2><p className="mt-2 text-sm">{selectedAppointment ? `Scheduled: ${formatWhen(selectedAppointment.start_at, selectedAppointment.timezone)}` : 'Schedule this responsibility on your connected calendar.'}</p><div className="mt-3 flex flex-wrap gap-2">{selectedAppointment ? selectedAppointment.canManage ? <><Button disabled={busy || !canWrite || !calendarReady || data?.binding?.calendar_id !== selectedAppointment.calendar_id} onClick={() => setEditor(selectedAppointment)}>Reschedule</Button><Button variant="outline" disabled={busy || !canWrite || !calendarReady || data?.binding?.calendar_id !== selectedAppointment.calendar_id} onClick={() => act('propose', { kind: 'cancel', appointmentId: selectedAppointment.id })}>Cancel appointment</Button></> : <p className="text-sm text-muted-foreground">Managed by another Google connection</p> : <Button disabled={busy || !canWrite || !calendarReady || ['complete', 'archived'].includes(selectedTask.status)} title={setupMessage || undefined} onClick={() => setEditor('new')}>Schedule this responsibility</Button>}</div></section>}
      {unresolved.length > 0 && <section aria-label="Calendar approvals" className="mt-8"><h2 className="font-heading text-xl font-semibold">Review before sending</h2><p className="mt-2 text-sm text-muted-foreground">Invitations and changes go to the guests below. Review the details shared outside Carestead.</p><div className="mt-4 space-y-4"><PaginatedList label="Calendar approvals" removal={{ individual: false, disabled: busy || !canWrite, canRemove: record => ['pending', 'failed'].includes(String(record.status)), description: 'Discard unsent calendar proposals. Processing or uncertain results remain for review.', remove: async ids => { for (const actionId of ids) if (!await act('reject', { actionId })) return false; return true; } }} records={unresolved} resetKey={state.selectedRecipient.id}>{unresolved.map((action) => <article key={action.id} className="rounded-[22px] border bg-card p-5 sm:p-6"><div className="flex flex-wrap items-center justify-between gap-2"><h3 className="font-heading text-lg font-semibold">{action.kind === 'create' ? 'New appointment' : action.kind === 'cancel' ? 'Cancel appointment' : 'Reschedule appointment'}</h3><Badge variant="outline">{action.status}</Badge></div><AppointmentPreview action={action} recipientName={state.selectedRecipient.display_name} />{calendarReady && !actionReady(action) && <p className="mt-3 text-sm text-muted-foreground">Select {action.payload.calendarName} above to manage this proposal.</p>}{action.error && <p className="mt-4 text-sm text-destructive">{action.error}</p>}<div className="mt-5 flex flex-wrap gap-2"><Button disabled={busy || !canWrite || !actionReady(action) || Boolean(action.payload.carePlan?.conflicts.length)} onClick={() => act('approve', { actionId: action.id })}>{busy ? <LoaderCircle className="animate-spin" /> : <CheckCircle2 />}{action.payload.googleConfirmed ? 'Approve remaining care changes' : action.status === 'pending' ? action.kind === 'cancel' ? 'Approve cancellation and notify guests' : action.kind === 'reschedule' ? 'Approve change and notify guests' : 'Approve and send invite' : 'Retry / check result'}</Button>{action.status === 'uncertain' && action.kind === 'reschedule' && <Button variant="outline" disabled={busy || !canWrite || !actionReady(action)} onClick={() => act('review_recovery', { actionId: action.id })}>Review remaining changes</Button>}{['pending', 'failed'].includes(action.status) && action.kind !== 'cancel' && <Button variant="outline" disabled={busy || !canWrite || !actionReady(action)} onClick={() => setProposal(action)}><Pencil /> Edit proposal</Button>}{['pending', 'failed'].includes(action.status) && <Button variant="outline" disabled={busy || !canWrite} onClick={() => act('reject', { actionId: action.id })}>Discard proposal</Button>}</div>{action.status === 'executing' && <p className="mt-3 text-xs text-muted-foreground">Processing with Google. If interrupted, check the result after two minutes.</p>}</article>)}</PaginatedList></div></section>}
      <section aria-label="Carestead appointments" className="mt-8"><h2 className="font-heading text-xl font-semibold">Care appointments</h2><p className="mt-2 text-sm text-muted-foreground">Appointments scheduled through Carestead. Changes made directly in Google are checked when preparing a change.</p><div className="mt-4 grid min-w-0 gap-4"><PaginatedList label="Care appointments" removal={{ individual: false, disabled: busy || !canWrite || !calendarReady, canRemove: record => Boolean(record.canManage) && record.calendar_id === data.binding?.calendar_id, description: 'Prepare cancellations for appointments you manage. Each cancellation still needs approval before guests are notified.', remove: async ids => { for (const appointmentId of ids) if (!await act('propose', { kind: 'cancel', appointmentId })) return false; return true; } }} records={activeAppointments} resetKey={state.selectedRecipient.id}>{!activeAppointments.length ? <div className="rounded-2xl border bg-card p-8 text-center"><CalendarDays className="mx-auto size-7 text-primary" /><p className="mt-3 font-medium">No calendar appointments yet</p><p className="mt-2 text-sm text-muted-foreground">Connect your account, choose a calendar, then schedule the next care appointment.</p></div> : activeAppointments.map((appointment) => <article key={appointment.id} className="min-w-0 rounded-2xl border bg-card p-5"><div className="flex flex-col justify-between gap-4 sm:flex-row"><div><h3 className="font-heading font-semibold">{appointment.title}</h3><p className="mt-1 text-sm">{formatWhen(appointment.start_at, appointment.timezone)} – {formatWhen(appointment.end_at, appointment.timezone)}</p><p className="mt-1 text-xs text-muted-foreground">{appointment.timezone}{appointment.location ? ` · ${appointment.location}` : ''}</p>{calendarLink(appointment.html_link) && <a className="mt-3 inline-flex items-center gap-1 text-sm text-primary underline underline-offset-4" href={calendarLink(appointment.html_link)!} target="_blank" rel="noreferrer">Open in Google Calendar <ExternalLink className="size-3" /></a>}</div><div className="flex flex-wrap items-start gap-2">{appointment.canManage && <><Button size="sm" variant="outline" disabled={busy || !canWrite || !calendarReady || data?.binding?.calendar_id !== appointment.calendar_id} onClick={() => setEditor(appointment)}>Reschedule</Button><Button size="sm" variant="ghost" disabled={busy || !canWrite || !calendarReady || data?.binding?.calendar_id !== appointment.calendar_id} onClick={() => act('propose', { kind: 'cancel', appointmentId: appointment.id })}>Cancel appointment</Button></>}{appointment.canManage && calendarReady && appointment.calendar_id !== data.binding?.calendar_id && <p className="text-xs text-muted-foreground">Select this appointment’s original calendar above to reschedule or cancel.</p>}{!appointment.canManage && <p className="text-xs text-muted-foreground">Managed by another Google connection</p>}</div></div></article>)}</PaginatedList></div></section>
      {data.actions.some((action) => action.status === 'executed') && <section className="mt-8" aria-label="Completed calendar actions"><h2 className="font-heading text-xl font-semibold">Confirmed actions</h2><ul className="mt-4 grid min-w-0 gap-3"><PaginatedList label="Confirmed calendar actions" layout="list" records={data.actions.filter(action => action.status === 'executed')} resetKey={state.selectedRecipient.id}>{data.actions.filter((action) => action.status === 'executed').map((action) => <li key={action.id} className="flex min-w-0 flex-wrap items-center gap-3 rounded-2xl border bg-card p-4 text-sm"><CheckCircle2 className="size-4 text-primary" /><span>{action.payload.title} · {action.kind === 'cancel' ? 'Cancelled' : action.kind === 'reschedule' ? 'Rescheduled' : 'Scheduled'}</span><Button size="sm" variant="outline" disabled={!canWrite} onClick={() => setUpdateAction(action)}>Prepare caregiver update</Button>{calendarLink(action.htmlLink) && <a className="text-primary underline" href={calendarLink(action.htmlLink)!} target="_blank" rel="noreferrer">View event</a>}</li>)}</PaginatedList></ul></section>}
    </>}
    {updateAction?.status === 'executed' && <section aria-label="Caregiver update after calendar change" className="mt-6"><h2 className="font-heading text-xl font-semibold">Share the confirmed changes</h2><p className="mt-2 text-sm text-muted-foreground">Review this completed change, choose the receiving caregiver and channel, then prepare a notification for approval. Nothing has been sent.</p><NotificationComposer key={updateAction.id} state={state} onChanged={onCompleted} initialDraft={calendarUpdateDraft(updateAction)} /></section>}
    <AppointmentEditor initialTask={selectedAppointment ? undefined : selectedTask} key={proposal?.id || (editor === 'new' ? 'new' : editor?.id || 'closed')} proposal={proposal} open={editor !== null || proposal !== null} appointment={editor === 'new' ? null : editor} state={state} busy={busy} error={error} onClose={() => { setEditor(null); setProposal(null); }} onSubmit={async (payload) => { if (await act(proposal ? 'edit' : 'propose', { ...payload, actionId: proposal?.id })) { setEditor(null); setProposal(null); } }} />
    <Dialog open={disconnectOpen} onOpenChange={setDisconnectOpen}><DialogContent><DialogHeader><DialogTitle>Disconnect Google Calendar?</DialogTitle><DialogDescription>This removes your saved Google credentials and calendar selections for all care recipients. Existing Google events remain. You can also revoke Carestead access in your Google account settings.</DialogDescription></DialogHeader><DialogFooter><Button variant="outline" onClick={() => setDisconnectOpen(false)}>Keep connected</Button><Button disabled={busy} onClick={async () => { if (await act('disconnect')) setDisconnectOpen(false); }}>Disconnect account</Button></DialogFooter></DialogContent></Dialog>
  </>;
}

function AppointmentPreview({ action, recipientName }: { action: CalendarAction; recipientName: string }) {
  const p = action.payload;
  return <><dl className="mt-4 grid gap-x-6 gap-y-3 text-sm sm:grid-cols-2">{[['Care recipient', recipientName], ['Title shared with guests', p.title], ['Organizer / calendar', `${p.organizer} · ${p.calendarName}`], ['Time zone', p.timeZone], ['Starts', formatWhen(p.start, p.timeZone)], ['Ends', formatWhen(p.end, p.timeZone)], ['Guests receiving updates', p.attendees.join(', ') || 'No guests — only your calendar'], ['Location', p.location || 'No location'], ...(action.kind === 'create' ? [['Your reminder', p.reminderMinutes ? `${p.reminderMinutes} minutes before` : 'None']] : [])].map(([label, value]) => <div key={label} className="min-w-0"><dt className="text-muted-foreground">{label}</dt><dd className="mt-1 break-words font-medium">{value}</dd></div>)}</dl>{p.carePlan && p.carePlan.changes.length > 1 && <section aria-label="Linked care changes" className="mt-5 rounded-xl border p-4"><h4 className="font-semibold">Appointment and linked responsibilities</h4><ul className="mt-3 space-y-3">{p.carePlan.changes.map(change => <li key={change.taskId}><p className="font-medium">{change.title} · {change.owner}</p><p className="text-sm text-muted-foreground">{formatWhen(change.before, p.timeZone)} → {formatWhen(change.after, p.timeZone)}</p></li>)}</ul><p className="mt-3 text-sm text-muted-foreground">Caregivers must reconfirm changed responsibilities. Checks use this care plan’s recorded availability, not personal-calendar free/busy.</p></section>}{Boolean(p.carePlan?.conflicts.length) && <div role="alert" className="mt-4 rounded-xl border border-destructive p-4"><p className="font-medium">Resolve before applying</p><ul>{p.carePlan!.conflicts.map((conflict, i) => <li key={i} className="mt-2 text-sm">{conflict}</li>)}</ul><p className="mt-2 text-sm">Update the care plan, then edit this proposal to refresh the preview.</p></div>}</>;
}

function AppointmentEditor({ open, appointment, proposal, state, busy, error, onClose, onSubmit, initialTask }: { initialTask?: DashboardState['tasks'][number]; open: boolean; appointment: CalendarAppointment | null; proposal: CalendarAction | null; state: DashboardState; busy: boolean; error: string; onClose: () => void; onSubmit: (payload: Record<string, unknown>) => Promise<void> }) {
  const rescheduling = Boolean(appointment || proposal?.kind === 'reschedule');
  const [timeZone, setTimeZone] = useState(proposal?.payload.timeZone || appointment?.timezone || state.selectedRecipient.timezone);
  const [title, setTitle] = useState(proposal?.payload.title || appointment?.title || initialTask?.title || 'Care appointment');
  const [start, setStart] = useState(proposal ? localInput(proposal.payload.start, proposal.payload.timeZone) : appointment ? localInput(appointment.start_at, appointment.timezone) : initialTask ? localInput(initialTask.due_at, state.selectedRecipient.timezone) : '');
  const [end, setEnd] = useState(proposal ? localInput(proposal.payload.end, proposal.payload.timeZone) : appointment ? localInput(appointment.end_at, appointment.timezone) : initialTask ? localInput(new Date(Date.parse(initialTask.due_at) + 3600000).toISOString(), state.selectedRecipient.timezone) : '');
  const [formError, setFormError] = useState('');
  return <Dialog open={open} onOpenChange={(value) => { if (!value) onClose(); }}><DialogContent className="max-h-[90vh] overflow-y-auto sm:max-w-xl"><DialogHeader><DialogTitle>{proposal ? 'Edit proposal' : appointment ? 'Reschedule appointment' : 'Schedule appointment'}</DialogTitle><DialogDescription>{appointment ? 'Choose the new time. Current Google event details and guests will appear in the approval preview.' : 'Prepare an invitation for caregiver review. Nothing is sent until you approve.'}</DialogDescription></DialogHeader><form onSubmit={async (event) => { event.preventDefault(); setFormError(''); const values = Object.fromEntries(new FormData(event.currentTarget)); try { await onSubmit({ ...values, title, startLocal: start, endLocal: end, kind: rescheduling ? 'reschedule' : 'create', appointmentId: appointment?.id, attendees: values.attendees ?? proposal?.payload.attendees.join(', '), location: values.location ?? proposal?.payload.location, reminderMinutes: Number(values.reminderMinutes ?? proposal?.payload.reminderMinutes ?? 0) }); } catch (e) { setFormError(e instanceof Error ? e.message : 'Could not prepare appointment.'); } }} className="space-y-4">
    {!rescheduling && <><label className="block text-sm font-medium">Link a responsibility<select name="taskId" disabled={Boolean(proposal)} defaultValue={proposal ? proposal.payload.taskId.startsWith("calendar-") ? "" : proposal.payload.taskId : initialTask?.id} className={`${selectClass} mt-2`} onChange={(event) => { const task = state.tasks.find((item) => item.id === event.target.value); if (task) { setTitle(task.title); setStart(localInput(task.due_at, timeZone)); setEnd(localInput(new Date(Date.parse(task.due_at) + 3600000).toISOString(), timeZone)); } }}><option value="">Create a new appointment responsibility</option>{state.tasks.filter((task) => !['complete', 'archived'].includes(task.status)).map((task) => <option key={task.id} value={task.id}>{task.title}</option>)}</select></label><label htmlFor="calendar-title" className="block text-sm font-medium">Event title<Input id="calendar-title" name="title" value={title} onChange={(event) => setTitle(event.target.value)} required maxLength={200} className="mt-2" /></label><p className="text-xs text-muted-foreground">Guests can see this title. Include only the care details you want to share.</p></>}
    <label htmlFor="calendar-timeZone" className="block text-sm font-medium">Time zone<Input id="calendar-timeZone" name="timeZone" value={timeZone} onChange={(event) => setTimeZone(event.target.value)} required maxLength={80} placeholder="America/Toronto" className="mt-2" /></label>
    <div className="grid gap-4 sm:grid-cols-2"><AppointmentDateTime label="Starts" value={start} onChange={setStart} /><AppointmentDateTime label="Ends" value={end} onChange={setEnd} /></div>
    {!rescheduling && <><label htmlFor="calendar-attendees" className="block text-sm font-medium">Guest email addresses<Input id="calendar-attendees" name="attendees" defaultValue={proposal?.payload.attendees.join(", ")} placeholder="maya@example.com, caregiver@example.com" maxLength={3000} className="mt-2" aria-describedby="calendar-invitation-help" /></label><p id="calendar-invitation-help" className="text-xs text-muted-foreground">Google may label a first invitation “unknown sender.” Guests can accept the invite to add the event, and mark the organizer as known or add the organizer’s email to Google Contacts for future invitations.</p><label htmlFor="calendar-location" className="block text-sm font-medium">Location<Input id="calendar-location" name="location" defaultValue={proposal?.payload.location} maxLength={500} className="mt-2" /></label><label className="block text-sm font-medium">Your calendar reminder<select name="reminderMinutes" className={`${selectClass} mt-2`} defaultValue={proposal?.payload.reminderMinutes ?? 30}><option value="0">None</option><option value="10">10 minutes before</option><option value="30">30 minutes before</option><option value="60">1 hour before</option><option value="1440">1 day before</option></select></label><p className="text-xs text-muted-foreground">Guests control their own reminders. No clinical notes are added automatically.</p></>}
    {(formError || error) && <p role="alert" className="text-sm text-destructive">{formError || error}</p>}<DialogFooter><Button type="button" variant="outline" onClick={onClose}>Cancel</Button><Button type="submit" disabled={busy}>{busy && <LoaderCircle className="animate-spin" />}{proposal ? 'Save and review' : rescheduling ? 'Review change' : 'Review invitation'}</Button></DialogFooter>
  </form></DialogContent></Dialog>;
}

function AppointmentDateTime({ label, value, onChange }: { label: string; value: string; onChange: (value: string) => void }) {
  const [open, setOpen] = useState(false);
  const [date = '', time = ''] = value.split('T');
  const selected = date ? new Date(`${date}T12:00:00`) : undefined;
  return <fieldset className="min-w-0 space-y-2"><legend className="text-sm font-medium">{label}</legend>
    <Popover open={open} onOpenChange={setOpen}>
      <PopoverTrigger render={<Button type="button" variant="outline" className="w-full justify-start font-normal" aria-label={`${label} date`} />}><CalendarDays className="size-4 text-primary" />{selected ? selected.toLocaleDateString('en-CA', { month: 'short', day: 'numeric', year: 'numeric' }) : 'Choose date'}</PopoverTrigger>
      <PopoverContent align="start" className="w-auto rounded-xl p-2"><Calendar mode="single" selected={selected} defaultMonth={selected} captionLayout="dropdown" startMonth={new Date(2020, 0)} endMonth={new Date(2100, 11)} className="[--cell-size:--spacing(9)]" onSelect={(day) => { if (day) { const next = `${day.getFullYear()}-${String(day.getMonth() + 1).padStart(2, '0')}-${String(day.getDate()).padStart(2, '0')}`; onChange(`${next}T${time || '09:00'}`); setOpen(false); } }} /></PopoverContent>
    </Popover>
    <TimePicker label={`${label} time`} value={time} onChange={(next) => onChange(`${date}T${next}`)} />
  </fieldset>;
}
